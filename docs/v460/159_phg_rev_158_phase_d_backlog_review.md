# 159# 158レビュー: Phase D バックログ再監査・優先度再編

> 対象: [158# バックログ監査 + Phase D 優先順位](158_phg_rpt_backlog_audit_and_phase_d_priorities.md)  
> 目的: ph2 終盤時点の「積み残し」を、**収益性・安全性・実装負債・運用性**の4軸で再評価し、実装進捗を反映した実行順序へ更新する。

---

## §0 総評（結論）

- 158# は全体として構造化が優秀で、特に sell 7重ゲート問題の可視化と P0/P1/P2 の分離は妥当。
- 一方で、**一部は既に実装済みのため「未対応」扱いが古い**（例: Oracle 基盤、`execute_trade` 実装）。
- 逆に、文書に明示されていないが、**運用上の即時リスク**が1件確認された（`trades_health` イベント記録部のフィールド不整合）。
- よって、158# は「方向性は正しい」が、**最新コード反映版への差分更新が必須**。

---

## §1 事実照合（158# 記載 vs 現在実装）

### 1.1 進捗反映が必要な項目

| 158# 項目 | 158# の扱い | 現状 | 評価 |
|---|---|---|---|
| P0-4 Oracle テスト | 新規スクリプト候補 | `scripts/v460/analysis/oracle_baseline.py` が存在 | **完了寄り（実行/運用定着フェーズ）** |
| P2-2 `OrderManager.execute_trade()` | 未実装ブロッカー | `ztb/trading/live_trader/components/order_manager.py` で live 実装あり | **文書更新要** |
| P2-4 run_fill_test 分割 | 未着手寄り | `scripts/v460/lib/*` に分割進展 (`lot_manager.py` 等) | **進行中（未完）** |
| P1-3 reprice ログ | `reprice_count` + `reprice_drift_bps` 追加提案 | `FillRecord.reprice_count` は実装済み、`reprice_drift_bps` は未確認 | **部分完了** |

### 1.2 優先度据え置きが妥当な項目

| 項目 | 評価 |
|---|---|
| sell offset A/B (P0-2) | 収益直結。引き続き高優先 |
| skip_sell_trending 分解効果測定 (P0-3) | 方向性は妥当。未評価なら継続 |
| skip_gate.py の `ztb/` 移設 (P2-5) | テスト安定性と構造整合上、依然有効 |
| VG 詳細ログ (P2-6) | `vg_triggered(bool)` だけでは分析不足。依然有効 |

---

## §2 重大指摘（新規発見）

## 2.1 `trades_health` イベント記録部の不整合

### 現象

- `run_fill_test.py` の `trades_health_alert` 生成で `th.latest_ts` / `th.age_hours` を参照している。
- しかし `TradesHealthResult` は `healthy / available_days / missing_days / stale_hours / message` のみ。
- 当該参照は `AttributeError` を誘発し、`except` に吸収されるため、**本来残るべき警告イベントが欠落**する恐れ。

### 影響

- retrain 品質劣化の監視証跡が不完全化。
- 「UNHEALTHY を検知したか」の運用検証が曖昧化。

### 優先度

- **P0（即修正）**

---

## §3 批判的レビュー（多角的視点）

## 3.1 収益性視点

- sell 側の「防御の螺旋」仮説は妥当。
- ただし offset 緩和は fill 改善と同時に adverse selection 増加を招くため、`fill_rate` 単独最適化は危険。
- 判定指標は最低でも `fill_rate / avg pnl30 / downside tail (p10 or worst decile)` の3点同時で管理すべき。

## 3.2 リスク管理視点

- P1-1 balance_forced 救済は必要だが、「救済モードの発火条件」と「解除条件」が曖昧だと損失固定化を招く。
- `Nサイクル連続 forced` + `ボラ閾値` + `最大救済時間` を必須にし、無限救済を禁止するべき。

## 3.3 ソフトウェア設計視点

- `run_fill_test.py` は分割が進んだ一方、行数増で依然高結合。
- ph2 稼働中は大規模分割より、`Facade維持 + 契約テスト先行` で段階移行する方が安全。

## 3.4 実験統制視点

- 158# の A/B 計画は有効だが、`variant` の系譜管理が弱い。
- `run_id -> config hash -> model hash -> git_sha` を1:1追跡できないと、後知恵分析が再現不能になる。

---

## §4 優先順位の再編（提案）

## 4.1 直近 48h（実行優先）

1. **P0-A**: `trades_health` フィールド不整合修正（監視証跡の欠落防止）
2. **P0-B**: sell offset A/B の評価軸を3指標化（fill率のみで判定しない）
3. **P0-C**: trending_down sell 効果測定を固定テンプレートで日次出力

## 4.2 次の 3–7日

4. **P1-A**: `reprice_drift_bps` を FillRecord へ追加（reprice効果を定量化）
5. **P1-B**: 最小 A/B 基盤（`variant_id` の fill_records 記録）
6. **P1-C**: VG 詳細ログ（trigger理由、velocity、regime、offset係数）

## 4.3 ph3 前に閉じるべき項目

7. **P2-A**: `execute_trade` 項目は「未実装」から「品質検証フェーズ」へ文書更新
8. **P2-B**: `run_fill_test` 分割は API 契約テストを先に固定し、安全に継続

---

## §5 範囲外だが高効果の改善提案

## 5.1 実験ガバナンス強化（再現性）

- `results/v460/fill_test/manifest.jsonl` を導入し、各 run の `variant/model/config/git_sha` を自動記録。
- これにより「何が効いたか」の監査可能性を担保。

## 5.2 ダウンサイド監視の標準化（安全）

- 日次で `worst-10% pnl` と `連敗長` を可視化し、一定閾値で自動 `safe mode`。
- 平均値依存を避け、実運用での毀損を抑える。

## 5.3 判定会議のゲート定義を固定化（意思決定品質）

- ph2→ph3 の進入条件を「必要十分条件」に再定義（例: `Oracle net>0`, `sell side worst-decile guard`, `運用イベント欠損なし`）。
- 閾値は文書化し、変更時は理由を必須記録。

---

## §6 158# への具体的修正提案（ドキュメント差分）

1. `P0-4` は「新規作成」表現を削除し、`oracle_baseline.py` 実行手順と判定閾値へ更新。  
2. `P2-2 execute_trade` は「未実装」から「実装済み・統合検証待ち」に更新。  
3. `P1-3` は `reprice_count` 実装済みを明記し、残課題を `reprice_drift_bps` のみに絞る。  
4. `§6.1` の行数・テスト件数など可変メタ情報は「概数」or 自動生成参照に変更。  
5. 重大事項として `trades_health` イベント記録不整合を追記（P0 扱い）。

---

## §7 結論

- 158# は戦略方針として有効であり、破棄ではなく**アップデート前提で継続利用**すべき。
- 現時点の最優先は、収益施策そのものよりも、**観測の欠損を埋める修正（trades_health 不整合）**。
- その上で、sell 緩和施策は「平均改善」ではなく「下振れ制御込み」で進めることが、短期高収益への最短経路。

---

## §8 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-02-24 | 初版: 158# の実装照合レビュー、優先度再編、範囲外改善提案を追加 |

---

## 追記: 158# / 159# に対するセカンドオピニオン (Gemini 3.1 Pro)

### 1. 159# (Codexレビュー) への評価と補足
159# の指摘（特に `trades_health` のバグ発見と、sell offset緩和に対するリスク警告）は極めて鋭く、実運用を強く意識した素晴らしいレビューである。優先順位の再編案（§4）も妥当であり、直ちに実行に移すべきである。

### 2. 見落とされている「致命的リスク」と「収益直結施策」

#### A. Oracleテスト (P0-4) の「結果評価」こそが真のP0である
159# では Oracle テストを「完了寄り」としているが、重要なのはスクリプトの存在ではなく**「その結果がどうだったか」**である。もし Oracle PnL（完全予測時の理論上限）から AS コストを引いた Net PnL がマイナスであれば、現在の Maker 戦略自体が破綻している（どんなに ML を改善しても勝てない）ことを意味する。他のいかなる改修よりも先に、Oracle テストの**結果評価と Go/No-Go 判断**を最優先で行うべきである。

#### B. sell offset 緩和 (P0-2) の代替案：非対称クオートと在庫偏重（Inventory Skewing）
159# でも警告されている通り、単なる sell offset の縮小は Adverse Selection の餌食になるだけである。収益に直結するアプローチとして、**「在庫状況に応じた非対称なクオート（Inventory Skewing）」**を提案する。
現状の `balance_forced` 救済（P1-1）は事後対応に過ぎない。在庫が一方に偏り始めた段階で、過剰な側の offset を広げ、不足している側の offset を狭める（あるいは同値撤退を許容する）ことで、在庫をニュートラルに保ちつつスプレッドを抜くマーケットメイカー本来の動きを実装すべきである。これを P0 に格上げすることを強く推奨する。

#### C. SkipGate sell 再訓練 (P0-1) 前の「構造的敗因」の特定
sell 側の PnL が極端に悪い原因を「旧データによるモデルの劣化」と決めつけるのは危険である。再訓練を盲目的に実行する前に、現在のモデルの**特徴量重要度（Feature Importance）と SHAP 値**を分析し、「なぜ sell 側だけが AS を回避できないのか」の構造的要因（例：マクロトレンドの影響、特定の特徴量のバグ）を特定するタスクを挟むべきである。

#### D. 「休むも相場」ロジックの欠如
158# のバックログには「エントリー条件の緩和」や「fill rate の向上」に関するタスクが多いが、高収益システムにおいて最も重要なのは**「期待値がマイナスの時に確実に取引を停止する」**ことである。133# で提案された「spread 狭小時の休む判定」や「worst hour-side の局所ルール学習」など、**防御的スキップロジックの精緻化**を P1 以上に組み込むべきである。

### 3. 結論とネクストアクション
159# の §4 (優先順位の再編) をベースとしつつ、以下の順序で実行することを推奨する。

1. **Day 0 (Go/No-Go)**: Oracle テストの結果を評価し、Maker 戦略の理論的優位性を証明する。
2. **Day 1 (観測修復)**: `trades_health` 不整合修正 (159# P0-A)。
3. **Day 2 (在庫管理)**: `balance_forced` の事後救済ではなく、事前予防としての Inventory Skewing ロジックの導入。
4. **Day 3 (分析と再訓練)**: sell 側モデルの SHAP 分析を実施した上で、SkipGate の再訓練 (158# P0-1) を行う。

---

## §9 追記レビュー（実装後・Git非依存確認）

> 依頼に基づき、Git 差分ではなく「実装痕跡の直接確認」で再レビューを実施。
> 対象は 158# 主要論点（P0/P1/P2）に限定。

### 9.1 確認結果サマリ

| 項目 | 判定 | 根拠 |
|---|---|---|
| `trades_health` フィールド不整合 | **対応済み** | `run_fill_test.py` のイベント詳細が `latest_day` / `stale_hours` 参照へ修正 |
| `reprice_drift_bps` 収集 | **対応済み** | `FillRecord` にフィールド追加 + `OrderMonitor`→`run_fill_test` へ伝搬 |
| A/B variant 記録 | **対応済み** | `FillTestConfig.ab_test_variant` と `FillRecord.ab_test_variant` の実装確認 |
| VG 詳細ログ（速度/VPIN/倍率） | **対応済み** | `vg_velocity_bps` / `vg_vpin` / `vg_boost_factor` を `FillRecord` 記録 |

### 9.2 評価（レビュワー見解）

- 158# で指摘した「観測欠損」系は、今回の実装で大きく改善した。
- 特に `trades_health_alert` の修正は、運用監視の信頼性を直接上げるため評価が高い。
- `reprice_drift_bps` と VG 詳細ログの追加により、今後の A/B 判断は「感覚」ではなく定量化しやすくなった。

### 9.3 残課題（実装済みだが運用未確認）

1. **データ実測の確認**: 新規フィールドが実データ JSONL に期待どおり出力されるかを最低1日分で確認。  
2. **分析スクリプト追従**: `ab_test_variant` / `reprice_drift_bps` / VG 新項目を集計側が利用しているか点検。  
3. **評価基準の固定**: sell offset A/B 判定を `fill_rate + avg_pnl30 + downside tail` の3指標必須に明文化。

### 9.4 優先度アップデート

- 旧 P0-A（`trades_health` 不整合修正）は **クローズ**。
- ~~次の最優先は **P0-B（A/B判定基準の固定）** と **P0-C（trending_down sell 実測評価）**。~~ → **160# で実装・実測完了**
  - P0-B: sell vs buy 3指標判定 → **FAIL** (fill_rate 36% 悪化 + p10 -5.39 < -5.0)
  - P0-C: trending_down sell 13件 → **FAIL** (avg_pnl30 +0.42 ✅ だが p10 -5.67 < -5.0)
  - 詳細: `160_phg_rpt_analysis_and_regime_tuning.md` §8 参照
- `variant` 記録が入ったため、次フェーズは「施策実装」より「統計的に誤判定しない運用ルール整備」を優先すべき。

### 9.5 変更履歴追記

| 日付 | 内容 |
|---|---|
| 2026-02-24 | 追記: 158# 実装後レビュー（Git非依存）。観測系4項目の対応済みを確認し、次優先を再設定 |
| 2026-02-24 | §9.4 更新: P0-B/C 160# で実装・実測完了。両項目 FAIL (p10 超過)。trending_down sell は暫定継続 |
