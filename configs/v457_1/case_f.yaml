version: '4.5.8-CaseF'
description: "v458 Frequency Control: High Threshold"

training:
  environment:
    initial_balance: 1000000.0
    transaction_cost: 0.0005
    max_position_size: 1.0
    
    # Base Control
    min_delta: 0.04
    max_ttl_steps: 30
    
    # Wrapper Control
    cooldown_steps: 60
    action_threshold: 0.8  # Very High Confidence only

    reward_settings:
      hold_grace: 20
      hold_ramp: 20.0
